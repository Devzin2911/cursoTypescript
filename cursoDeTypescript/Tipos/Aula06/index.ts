/**
 * Aula sobre o componente VOID
 * Basicamente isso serve para voce indicar que uma função 
 * não vai retornar nada, ou seja, ela não precisa retornar um
 * elemento para funcionar
 * 
 */

let pdd = (msg: string): void => {
    console.log(msg);
}